{
  "description": "docdd: docdd のプロジェクトで、取り消しにくい git 操作（git add -A・git commit --amend・強制 push など）と、秘密の値（API キー・秘密鍵・.env・ログイン状態のファイル）の入ったコミットを止め、rm -r／rm -f の前に注意書きを出す。セッションの開始時には、プラグインの版と置いた雛形の版がずれていれば /docdd:update-kit を案内する（何も直さない）",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/scripts/guard-bash.mjs\"",
            "timeout": 30,
            "statusMessage": "docdd: コマンドを確かめています"
          }
        ]
      }
    ],
    "SessionStart": [
      {
        "matcher": "startup|resume",
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/scripts/notify-update.mjs\"",
            "timeout": 10,
            "statusMessage": "docdd: 雛形の版を見ています"
          }
        ]
      }
    ]
  }
}
