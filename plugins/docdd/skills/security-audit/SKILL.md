---
name: security-audit
description: セキュリティの穴を探して報告し、直すのは 1 件ずつ運営者の「はい」を得てから。公開前や、ログイン・課金・外部連携を触ったあとに使う。--audit は報告のみ。
argument-hint: "[対象パス] [--audit]"
---

> **前提（docdd）**
> - 最初に `tasks/BACKLOG.md` と、`AGENTS.md`（無ければ `CLAUDE.md`）の「検証コマンド」表があるかを確かめる。どちらかが無ければファイルを作らず、「先に `/docdd:init` を実行してください」と伝えて止まる。
> - `/docdd:init`・`/docdd:release`・`/docdd:update-kit` は運営者が自分で打つコマンドで、Claude のスキル一覧には出ない。一覧に無いことを不具合として伝えない。止まるときは要望の中身の検討を始めない。
> - 表も約束も `AGENTS.md` にある（「検証コマンド」「反映コマンド」「スキルへの追加指示」と、「キット共通の約束」の「変更影響 → 必須の検証」・Definition of Done・規約）。v0.13 以前に導入したプロジェクトでは `CLAUDE.md` と `.claude/rules/docdd-kit.md` にあるので、そちらを読む。`/docdd:update-kit` で移せる。
> - `AGENTS.md`「スキルへの追加指示」にこのスキルの行があれば、本文より優先する。
> - 表の値が「無い」の行は飛ばし、二重波かっこ（`{{…}}`）のままの行は未記入として実行しない。どちらも理由を報告する（黙って省略しない）。
> - 課金・AI・ジョブ・決済など、このプロジェクトに無い機能についての手順は飛ばして「該当なし」と報告する。要決定には積まない。

# security-audit：穴を探して、確かめて、直すか判断を仰ぐ

**推測を報告しない。** セキュリティの指摘は「ありそう」で書くと嘘が混ざり、本物の指摘まで信用されなくなる。**攻撃者の手順を1本書き、コードで到達を確かめてから**報告する。

**勝手に直さない・勝手に方針を変えない。** 穴の埋め方には「実装で閉じる」ものと「運営者が決める」ものがある。§4の表で機械的に振り分け、実装で閉じるものも**1件ずつ運営者の「はい」を得てから**直す（§5 手順5）。

**分母を先に出す。** 何を見なかったかを言えない監査は、緑に見えても意味がない。§1で入口を機械的に数え上げ、§5 手順7で1つずつ消し込む。

見落としやすい形は `references/pitfalls.md` に「症状 → 見る場所 → 対策」でまとめてある。§2・§3に入る前に読む。

## 1. 攻撃面の棚卸し（最初にやる・省略しない）

**コマンドで数え上げる。** 手で列挙すると漏れる。

フレームワークによってファイルの置き場と目印は違う。`< >` はこのプロジェクトのものへ読み替えて実行する。

```bash
find <アプリのディレクトリ> -name '<APIハンドラのファイル名>'                 # API・公開ハンドラ（メール確認・webhook のような未認証の入口を含む）
grep -rln '<サーバー側処理の目印>' <サーバー側処理のディレクトリ>              # サーバー側の処理（Server Action など、ブラウザから直接呼べるもの）
find <アプリのディレクトリ> -name '<ページのファイル名>' -not -path '<認証必須の領域>'  # 未認証で開けるページ
ls <migration のディレクトリ> | tail -5                                      # 直近のスキーマ変更
git log --oneline -20 --name-only | grep -E '<API・サーバー側処理のパス>'      # 直近で増えた入口
```

出た一覧を**そのまま報告の骨子にする**。各項目は最後に「見た（根拠）」「穴あり」「範囲外（理由）」のどれかで埋まっていること。

## 2. 前提（このプロジェクトの守り方）

既に**検査で守っている**ものは「無い」ではなく「**検出器が生きているか**」を見る。

下の表の検出器はこのプロジェクトのものへ読み替える。対応する検出器が無い行は、無いことを理由つきで報告する（黙って省略しない）。

| 守り | 検出器（例。このプロジェクトのものへ読み替える） | 破れたときの症状 |
|---|---|---|
| 行の越境（RLS・GRANT など、使っている DB の仕組みの権限） | 実 DB で行の権限と役割の権限を検査するテスト | 他人のデータが見える／ログイン利用者の役割が直接書ける |
| 利用者どうしの干渉 | 利用者の分離を検査する実 DB テスト | 1人の大量投入で他人が止まる |
| 外向き副作用 | 外向きチャネル（外部サービスへの送信・メール・課金）の環境ガード ＋ そのテスト | 非本番から本物の送信・メール・課金が出る |
| 定時処理の認可 | 定時処理の入口の認可テスト | 秘密無しで定時処理が叩ける |
| サーバー側処理の到達性 | 呼ばれないサーバー側処理を検出するテスト | 呼ばれない処理が残り攻撃面になる |
| 環境ごとの鍵 | 環境と鍵の対応を検査するテスト | ローカルの鍵で本番を叩く |
| CSP（nonce） | `AGENTS.md`「検証コマンド」表の『ビルド』行で作り、『本番モード起動』行で起動して、実ブラウザで公開ページのコンソールエラーまで見る | 事前描画で script が動かず**200のまま画面だけ壊れる** |
| 依存の脆弱性 | `AGENTS.md`「検証コマンド」表の『依存の脆弱性』行（npm は `scripts/audit-allowlist.json` の据え置きつき。npm 以外のコマンドはこの一覧を読まず、据え置きは `tasks/BACKLOG.md` の要決定にある） | 既知CVEが本番依存に入る |
| 秘密値の露出 | 外部サービスの生エラーを保存する列があれば利用者向けの読み取りから除外する・利用者向けエラーへの変換・ログ送信前の秘匿処理 | 外部サービスの生文言・鍵がブラウザやログへ出る |

**「テストがある」で安心しない。** そのテストが**実物を通しているか**を確かめる（スタブ・モックが実物と違う形をしていると、穴を1件も検出できない。`references/pitfalls.md`）。

## 3. 見る場所（全部見る。畳まない）

分割して回すなら、下の見出しをそのまま単位にする。

1. **認可（誰のデータか）** — 入口で本人か。所有権は**DB 問い合わせの条件（SQL なら where）**にあるか
   （アプリ側の突き合わせだけだと競合で抜ける）。ID を受け取る経路は毎回本人のものか確かめているか。
2. **未認証で叩ける入口** — API・メール確認・パスワード再設定・webhook のような認証前の入口・公開ページ。
   認証が無いのは仕様として正しいか。**そこで何が書き換わるか**（Cookie・DB・課金・メール送信）。
3. **費用の出る経路** — AI・外部API・メール送信・決済。契約（プラン・上限）のガードを通るか（正本は `docs/requirements/`）。
   **入口だけでなく実行時（ジョブを取り出す時点）でも見ているか**。上限・冪等キーを回避できないか。
4. **秘密値** — トークン・APIキー・定時処理の秘密・暗号鍵。
   ログ・**例外オブジェクトのプロパティ**・エラー本文・DB・応答・エラー監視サービスのcontextへ出ていないか。
5. **入口の検査** — Origin（CSRF）・署名（決済 webhook）・state/PKCE（OAuth）・人間確認・
   スキーマ検証の未知フィールド・**本文サイズの上限**。
6. **境界の値** — 利用者が渡す ID・URL・`next`・コード。オープンリダイレクト、SSRF、
   パストラバーサル、Cookie注入。
7. **保存物へのアクセス** — ファイル保存先（バケット等）が非公開か、ポリシーはあるか、
   **署名URLを誰の path に発行しているか**（クライアントから path を受け取っていないか）。
8. **AIへの入力と出力** — 利用者の文字列がプロンプトへ入る経路。**出力を信用してDB・外部APIへ
   流していないか**。合成後の**総量**に上限があるか（個々のフィールドに上限があっても合計は別）。
9. **時間差（TOCTOU）** — 確認から実行までに変わる状態（契約・上限・所有権）。
10. **依存とビルド** — `AGENTS.md`「検証コマンド」表の『依存の脆弱性』行を実際に走らせ、**据え置いている脆弱性の理由が今も成り立つか**
    （到達経路が本番に無いと言えるか。直った版が出ていないか）を ID ごとに確かめる。据え置きは、npm なら `scripts/audit-allowlist.json` の `ids`・`why`・`until`、npm 以外なら `tasks/BACKLOG.md` の要決定にある。
11. **応答ヘッダ・CSP** — `AGENTS.md`「検証コマンド」表の『ビルド』行で作り、『本番モード起動』行で起動して、実ブラウザで公開ページのコンソールエラーまで
    見る（変更影響表（`AGENTS.md`「変更影響 → 必須の検証」）の『本番ビルドでしか出ない設定』行）。CSPの許可先が増えていないか。
12. **可用性** — 未認証で無制限に回せる処理（ログ肥大・DB書き込み・外部API）。

## 4. 直すか、判断を仰ぐか

| 条件 | どうする |
|---|---|
| **明確な穴**で、直しても利用者から見た正常系の挙動が変わらない（検査の追加・条件の追加・値の秘匿） | **運営者の「はい」を得て直す**（§5 手順5）。回帰テストを同じコミットで |
| 直すと**利用者に見える挙動が変わる**（できていたことができなくなる・文言が変わる） | **要決定へ**（暫定案つき。書き方は §5 手順6） |
| **外部サービスの設定**が要る（決済・DB・人間確認・連携先 API の管理画面） | **要決定へ**。手順を書いて依頼する |
| 方針そのものの変更（列挙対策の可否・保持期間・公開範囲） | **要決定へ**。判断の材料を3点以上並べる |
| **本番データの露出が疑われる**（既に漏れた可能性） | **止めて報告**。調査手順を示し、勝手に消さない・勝手に通知しない |
| 反証できた（到達不能・既に守られている） | **報告に「棄却」として残す**。理由も書く |

## 5. 手順

0. **緑を確認**: `AGENTS.md`「検証コマンド」表の『型検査』『lint』『単体・DBテスト』行を回す。テストの行は実行件数も見て、0 件なら緑にしない（例: Unity はテストが 0 件でも終了コード 0 で終わる。`-testResults` の XML の `total` を見る）。
1. **棚卸し**（§1）。**分母の一覧を作る。**
2. **探索**（§3の1〜12）。各候補に *攻撃者の手順 / file:line / 到達の根拠 / 影響 / 深刻度*。
3. **反証（必須）**: 候補ごとに「どこかの層で止まらないか」をコードで確かめる。**止まるなら棄却**。
   ここを飛ばした報告は出さない。**可能なら実際に動かして確かめる**（例: ログ出力を実際に走らせて、秘密値が出るかを見る）。
4. **振り分け**（§4）。
5. **直す（1件ずつ・承知を得てから）**: 直す候補ごとに、次の5つを示して**運営者の「はい」を得てから**直す。
   - 穴（file:line と、何ができてしまうか）
   - 攻撃者の手順
   - 差分（入れる予定の変更）
   - 回帰テスト（穴を再現するテストの内容）
   - 利用者への影響（正常系で見え方が変わらないこと。変わるなら §4 で要決定へ回す）

   「はい」を得たら、**穴を再現する回帰テストを先に書いて赤いことを確かめ**、修正を入れて緑にし、**修正を戻すと赤くなることを確認する**。検証は変更影響表（`AGENTS.md`「変更影響 → 必須の検証」）の該当行。コミットの前に `/docdd:doc-sync` を回し（権限の決まり・エラーの応答など、仕様書に書いてある所が変わったら直す）、テストと修正と仕様書を同じコミットにする。直す文書に着手前からの未コミットの変更（運営者の書き換え）があれば、その文書は直さず、直す中身と文書を報告に書く。`git status` を見て、この修正で変えたファイルだけをパスを明示して `git add` する（着手前からの変更は含めない。運営者の書き換えは `/docdd:add-task`（何も書かずに打つ）→ `/docdd:dev-loop` で取り込む）。
   「はい」以外なら直さず、要決定へ回す（手順6）。
   - **無人（`/loop`・`/schedule`）で動いているとき**は、コードもテストも変えない。直す候補は上の5つを添えて要決定に起票し（手順6）、報告する（手順8）だけにする。
   - `--audit` のときはこの手順を飛ばし、直す候補は報告に載せる。
6. **要決定を書く**: `tasks/BACKLOG.md` の「要決定・外部準備（ユーザー作業）」へ、運用ルールの見本の書式（`**D-<番号>: <論点>**` と `- 状態: 未決` の行を含む）で、案A/案B＋推奨を書く。番号はアーカイブも含めた既存の D-番号の次。
7. **漏れの点検（省略しない）**: §1の一覧を1行ずつ消し込み、**「見なかったもの」を名指しで報告する**。
   「全部見た」と書かない——書けるのは消し込みが終わったときだけ。
8. **報告**: 直した穴／承知待ちで要決定へ回した穴／要決定／**棄却した候補と理由**／**見なかった範囲**。

## やってはいけないこと

- **運営者の「はい」を得ずに直す**（無人で動いているときも同じ。起票と報告だけにする）。
- **検査を消して「直した」ことにする**（落ちるテストを消す・条件を緩める）。
- **秘密値を報告・コミット・テストへ書く**。再現手順は値を伏せる。
- 本番・検証環境（staging）のデータで試す。再現は**ローカルのテストデータ**で行う。
- 反証していない指摘を報告に混ぜる。
- 「多層防御」を理由に利用者の利便性を落とす変更を勝手に入れる（要決定へ）。
- **観点を畳んで回す**（§3の12観点を4つ以下にまとめない。`references/pitfalls.md`）。

## Definition of Done

- [ ] §1の棚卸しを**コマンドで**行い、一覧を報告の骨子にした
- [ ] §3の1〜12をすべて回した（回せなかったものは理由つきで報告した）
- [ ] 候補すべてに**反証**を通した（棄却は理由つきで残した）
- [ ] 直した穴はすべて、5つを示して運営者の「はい」を得てから直した
- [ ] 直した穴には**穴を再現する回帰テスト**があり、修正を戻すと赤くなることを確認した
- [ ] 判断が要るものは「要決定」へ案つきで起票した（実装していない）
- [ ] §1の一覧を消し込み、**見なかった範囲を名指しで**報告した
- [ ] 秘密値を報告・コード・テストへ書いていない
- [ ] ＋ Definition of Done（`AGENTS.md`）（直した穴があるとき）
