{
  "name": "docdd-dev-kit-repo",
  "private": true,
  "description": "docdd プラグインの配布リポジトリで使う検査とテスト（プラグインには同梱しない）",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "node scripts/run-tests.mjs",
    "check": "node scripts/check-skill-refs.mjs && node scripts/check-version-stamps.mjs && node scripts/check-version-bump.mjs",
    "check:urls": "node scripts/check-urls.mjs"
  }
}
